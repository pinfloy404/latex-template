services:
  latex-template:
    image: texlive/texlive:TL2025-historic
    container_name: latex-template
    working_dir: /latex-template/tex
    volumes:
    - ./:/latex-template
    command: latexmk -pdf -outdir=out main.tex