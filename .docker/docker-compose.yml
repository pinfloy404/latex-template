services:
  latex-template:
    build: .
    container_name: latex-template
    working_dir: /latex-template/tex
    volumes:
    - ./:/latex-template
    command: latexmk -pdf -shell-escape -outdir=out main.tex